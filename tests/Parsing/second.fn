# Parsing Test 2.
# Functions called after other Functions,
# Functions called inside expressions,
# and ArgMaps

# functions
decl func;
decl square;


decl x = list[1, 2, 3];
fn func[x, y] {
    cout `Hello world`;
    cout (+ x y);
}

append[func, x];
index[x, 3][2, 3];

fn square[x] {
    return (* x x);
}

decl y = (+ square[12] square[13]);
decl sqsq = square[square[12]];