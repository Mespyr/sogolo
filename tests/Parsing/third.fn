# Third Parsing Test
fn test[x, y] {
    return (+ (* x y) (/ x y));
}
let test_return = test[
    test[12, 13], 
    test[14, 15]
];

fn inside_list_func[x] {
    return test[x, x];
}
let ls = list[inside_list_func];

let inner = test[
    index[ls, 0][12], test_return
];

println test_return;